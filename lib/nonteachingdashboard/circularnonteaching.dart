import 'package:flutter/material.dart';

class circularnonteaching extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Circular - Non Teaching"),
      ),
      body: Center(
        child: Text(
          "Circular Page for Non-Teaching Staff",
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}
