import 'package:flutter/material.dart';

class LeaveReportsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Leave Reports'),
      ),
      body: Center(
        child: Text(
          'Leave Reports Page',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
