import 'package:flutter/material.dart';

class SyllabusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Syllabus")),
      body: Center(child: Text("Syllabus Page")),
    );
  }
}
