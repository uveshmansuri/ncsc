import 'package:flutter/material.dart';

class faculty_home extends StatelessWidget {
  const faculty_home({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text("Faculty Home"),
      ),
    );
  }
}
